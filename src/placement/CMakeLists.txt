find_package( Boost 1.59 )
INCLUDE_DIRECTORIES( ${Boost_INCLUDE_DIR} )
add_library (placement library.cpp cells.cpp lef.cpp)
target_include_directories ( placement PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries( placement ${Boost_LIBRARIES} entity standard_cell netlist )